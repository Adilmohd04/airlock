# dependencies
node_modules/

# build output
dist/
*.tsbuildinfo

# graphify cache (the graph itself is kept, the cache is regenerable)
graphify-out/cache/

# live git worktrees for in-flight background subagents — not repo content
.claude/worktrees/

# editor / OS
.DS_Store
Thumbs.db
*.local

# env — never commit
.env
.env.*
!.env.example

# mirrored local-model weights — hundreds of MB, never committed.
# Populate with: node scripts/fetch-models.mjs   (see the script header)
apps/airlock/public/models/
